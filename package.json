{
  "name": "frontier-signals",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "check": "node scripts/check.mjs",
    "deploy:dry-run": "WRANGLER_LOG_PATH=.wrangler/wrangler.log wrangler deploy --dry-run --strict",
    "deploy": "WRANGLER_LOG_PATH=.wrangler/wrangler.log wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.119.0"
  }
}
